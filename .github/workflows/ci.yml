# //////////////////////////////////////////////////////////////////////////
# //
# //  CypherEngine Source Code
# //  Copyright (c) 2026 Karlo Siric. All rights reserved.
# //
# //  File: .github/workflows/ci.yml
# //  Purpose: Defines GitHub Actions continuous integration for CypherEngine.
# //  Details: This workflow is the first automated safety net for cross-platform builds
# //           and tests. Keep it deterministic, readable, and close to the local build
# //           commands.
# //
# //  History:
# //  - Created by Karlo Siric on 2026-06-12
# //
# //  This file is proprietary and confidential. See LICENSE for details.
# //
# //////////////////////////////////////////////////////////////////////////

name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  VCPKG_DOWNLOADS: ${{ github.workspace }}/.vcpkg-downloads
  # Hosted runners expose enough logical CPUs to exhaust memory when every test
  # translation unit compiles concurrently, especially under ASan/UBSan.
  CYPHER_CI_BUILD_JOBS: 2

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dependency-manifest:
    name: Dependency Manifest
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Bootstrap pinned vcpkg
        run: |
          mkdir -p "$VCPKG_DOWNLOADS"
          cmake -P cmake/CypherBootstrapVcpkg.cmake

      - name: Resolve approved dependency graph
        run: |
          ./.deps/vcpkg/vcpkg install --dry-run \
            --x-manifest-root=. \
            --x-install-root=out/vcpkg-manifest-check \
            --x-feature=archive-tools \
            --x-feature=asset-import-fallback \
            --x-feature=audio \
            --x-feature=benchmarks \
            --x-feature=editor-support \
            --x-feature=image-import \
            --x-feature=math \
            --x-feature=mesh-pipeline \
            --x-feature=networking \
            --x-feature=profiling \
            --x-feature=scripting \
            --x-feature=security \
            --x-feature=shader-tools \
            --x-feature=tests \
            --x-feature=text \
            --x-feature=texture-pipeline

  build-and-test:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    defaults:
      run:
        shell: bash

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Ubuntu Debug
            os: ubuntu-latest
            build_type: Debug
          - name: Ubuntu Release
            os: ubuntu-latest
            build_type: Release
          - name: Windows Debug
            os: windows-latest
            build_type: Debug
          - name: Windows Release
            os: windows-latest
            build_type: Release
          - name: macOS Debug
            os: macos-latest
            build_type: Debug
          - name: macOS Release
            os: macos-latest
            build_type: Release

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf autoconf-archive automake libtool libtool-bin libltdl-dev

      - name: Install macOS build dependencies
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install autoconf autoconf-archive automake libtool
          if ! command -v libtoolize >/dev/null 2>&1 && command -v glibtoolize >/dev/null 2>&1; then
            sudo ln -sf "$(command -v glibtoolize)" /usr/local/bin/libtoolize
          fi

      - name: Bootstrap pinned vcpkg
        run: |
          mkdir -p "$VCPKG_DOWNLOADS"
          cmake -P cmake/CypherBootstrapVcpkg.cmake

      - name: Cache vcpkg artifacts
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/vcpkg/archives
            .vcpkg-downloads
          key: vcpkg-${{ runner.os }}-${{ hashFiles('vcpkg.json', '.gitmodules', 'cmake/CypherBootstrapVcpkg.cmake') }}
          restore-keys: |
            vcpkg-${{ runner.os }}-

      - name: Configure
        run: |
          configure_command=(
            cmake -S . -B build
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
            -DCYPHERENGINE_BUILD_TESTS=ON
            -DVCPKG_MANIFEST_FEATURES=tests
            "-DCMAKE_TOOLCHAIN_FILE=$PWD/.deps/vcpkg/scripts/buildsystems/vcpkg.cmake"
          )
          for attempt in 1 2 3; do
            "${configure_command[@]}" && break
            status=$?
            if [[ "$attempt" == "3" ]]; then
              exit "$status"
            fi
            echo "Configure failed; retrying in $(( attempt * 20 )) seconds..."
            sleep $(( attempt * 20 ))
          done

      - name: Build
        run: cmake --build build --config ${{ matrix.build_type }} --parallel "$CYPHER_CI_BUILD_JOBS"

      - name: Test
        run: ctest --test-dir build -C ${{ matrix.build_type }} --output-on-failure --no-tests=error --timeout 120

  linux-sanitizers:
    name: Ubuntu ASan UBSan
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        shell: bash

    env:
      ASAN_OPTIONS: detect_leaks=1:halt_on_error=1
      UBSAN_OPTIONS: halt_on_error=1

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Linux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf autoconf-archive automake libtool libtool-bin libltdl-dev

      - name: Bootstrap pinned vcpkg
        run: |
          mkdir -p "$VCPKG_DOWNLOADS"
          cmake -P cmake/CypherBootstrapVcpkg.cmake

      - name: Cache vcpkg artifacts
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/vcpkg/archives
            .vcpkg-downloads
          key: vcpkg-${{ runner.os }}-sanitize-${{ hashFiles('vcpkg.json', '.gitmodules', 'cmake/CypherBootstrapVcpkg.cmake') }}
          restore-keys: |
            vcpkg-${{ runner.os }}-

      - name: Configure
        run: |
          configure_command=(
            cmake -S . -B build-sanitize
            -DCMAKE_BUILD_TYPE=Debug
            -DCYPHERENGINE_BUILD_TESTS=ON
            -DVCPKG_MANIFEST_FEATURES=tests
            "-DCMAKE_C_FLAGS=-fsanitize=address,undefined -fno-omit-frame-pointer"
            "-DCMAKE_CXX_FLAGS=-fsanitize=address,undefined -fno-omit-frame-pointer"
            -DCMAKE_EXE_LINKER_FLAGS=-fsanitize=address,undefined
            "-DCMAKE_TOOLCHAIN_FILE=$PWD/.deps/vcpkg/scripts/buildsystems/vcpkg.cmake"
          )
          for attempt in 1 2 3; do
            "${configure_command[@]}" && break
            status=$?
            if [[ "$attempt" == "3" ]]; then
              exit "$status"
            fi
            echo "Configure failed; retrying in $(( attempt * 20 )) seconds..."
            sleep $(( attempt * 20 ))
          done

      - name: Build
        run: cmake --build build-sanitize --config Debug --parallel "$CYPHER_CI_BUILD_JOBS"

      - name: Test
        run: ctest --test-dir build-sanitize -C Debug --output-on-failure --no-tests=error --timeout 120

  render-asset-tools:
    name: ${{ matrix.name }} Render Asset Tools
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    defaults:
      run:
        shell: bash

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Ubuntu
            os: ubuntu-latest
          - name: Windows
            os: windows-latest
          - name: macOS
            os: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf autoconf-archive automake libtool libtool-bin libltdl-dev

      - name: Install macOS build dependencies
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install autoconf autoconf-archive automake libtool
          if ! command -v libtoolize >/dev/null 2>&1 && command -v glibtoolize >/dev/null 2>&1; then
            sudo ln -sf "$(command -v glibtoolize)" /usr/local/bin/libtoolize
          fi

      - name: Bootstrap pinned vcpkg
        run: |
          mkdir -p "$VCPKG_DOWNLOADS"
          cmake -P cmake/CypherBootstrapVcpkg.cmake

      - name: Cache vcpkg artifacts
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/vcpkg/archives
            .vcpkg-downloads
          key: vcpkg-${{ runner.os }}-render-asset-tools-${{ hashFiles('vcpkg.json', '.gitmodules', 'cmake/CypherBootstrapVcpkg.cmake') }}
          restore-keys: |
            vcpkg-${{ runner.os }}-

      - name: Configure render asset tools
        run: |
          configure_command=(
            cmake -S . -B build-render-asset-tools
            -DCMAKE_BUILD_TYPE=Debug
            -DCYPHERENGINE_BUILD_TESTS=ON
            -DCYPHERENGINE_BUILD_TOOLS=ON
            "-DVCPKG_MANIFEST_FEATURES=image-import;shader-tools;tests"
            "-DCMAKE_TOOLCHAIN_FILE=$PWD/.deps/vcpkg/scripts/buildsystems/vcpkg.cmake"
          )
          for attempt in 1 2 3; do
            "${configure_command[@]}" && break
            status=$?
            if [[ "$attempt" == "3" ]]; then
              exit "$status"
            fi
            echo "Render-asset-tools configure failed; retrying in $(( attempt * 20 )) seconds..."
            sleep $(( attempt * 20 ))
          done

      - name: Build ResourceCompiler and focused tests
        run: |
          cmake --build build-render-asset-tools --config Debug --parallel "$CYPHER_CI_BUILD_JOBS" --target \
            CypherResourceCompiler \
            cypher_common_tier1_fileio_tests \
            cypher_common_vfs_tests \
            cypher_common_render_asset_format_tests \
            cypher_common_cooked_resource_format_tests \
            cypher_common_cooked_shader_format_tests \
            cypher_common_cooked_texture_format_tests \
            cypher_common_cooked_material_format_tests \
            cypher_toolframework_core_tests \
            cypher_toolframework_cli_tests \
            cypher_toolframework_compiler_tests \
            cypher_shader_compiler_tests \
            cypher_texture_compiler_tests \
            cypher_material_compiler_tests

      - name: Test render asset toolchain
        run: |
          ctest --test-dir build-render-asset-tools -C Debug \
            --output-on-failure \
            --no-tests=error \
            --timeout 120 \
            -R "^(cypher_common_tier1_fileio_tests|cypher_common_vfs_tests|cypher_common_render_asset_format_tests|cypher_common_cooked_resource_format_tests|cypher_common_cooked_shader_format_tests|cypher_common_cooked_texture_format_tests|cypher_common_cooked_material_format_tests|cypher_toolframework_core_tests|cypher_toolframework_cli_tests|cypher_toolframework_compiler_tests|cypher_shader_compiler_tests|cypher_texture_compiler_tests|cypher_material_compiler_tests|cypher_resource_compiler_help|cypher_resource_compiler_version|cypher_resource_compiler_list_compilers|cypher_resource_compiler_describe_shader|cypher_resource_compiler_describe_texture|cypher_resource_compiler_describe_material|cypher_resource_compiler_list_formats_json|cypher_resource_compiler_smoke|cypher_resource_compiler_output_contract)$"

  benchmark-build:
    name: ${{ matrix.name }} Bench Build
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    defaults:
      run:
        shell: bash

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Ubuntu
            os: ubuntu-latest
          - name: Windows
            os: windows-latest
          - name: macOS
            os: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf autoconf-archive automake libtool libtool-bin libltdl-dev

      - name: Install macOS build dependencies
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install autoconf autoconf-archive automake libtool
          if ! command -v libtoolize >/dev/null 2>&1 && command -v glibtoolize >/dev/null 2>&1; then
            sudo ln -sf "$(command -v glibtoolize)" /usr/local/bin/libtoolize
          fi

      - name: Bootstrap pinned vcpkg
        run: |
          mkdir -p "$VCPKG_DOWNLOADS"
          cmake -P cmake/CypherBootstrapVcpkg.cmake

      - name: Cache vcpkg artifacts
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/vcpkg/archives
            .vcpkg-downloads
          key: vcpkg-${{ runner.os }}-bench-${{ hashFiles('vcpkg.json', '.gitmodules', 'cmake/CypherBootstrapVcpkg.cmake') }}
          restore-keys: |
            vcpkg-${{ runner.os }}-

      - name: Configure benchmarks
        run: |
          configure_command=(
            cmake -S . -B build-bench
            -DCMAKE_BUILD_TYPE=Release
            -DCYPHERENGINE_BUILD_BENCHMARKS=ON
            -DVCPKG_MANIFEST_FEATURES=benchmarks
            "-DCMAKE_TOOLCHAIN_FILE=$PWD/.deps/vcpkg/scripts/buildsystems/vcpkg.cmake"
          )
          for attempt in 1 2 3; do
            "${configure_command[@]}" && break
            status=$?
            if [[ "$attempt" == "3" ]]; then
              exit "$status"
            fi
            echo "Configure benchmarks failed; retrying in $(( attempt * 20 )) seconds..."
            sleep $(( attempt * 20 ))
          done

      - name: Build all benchmark targets
        run: cmake --build build-bench --config Release --parallel "$CYPHER_CI_BUILD_JOBS"

      - name: Report benchmark system info
        run: |
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            ./build-bench/bin/Release/cypher_perf_system_report.exe
          else
            ./build-bench/bin/cypher_perf_system_report
          fi
