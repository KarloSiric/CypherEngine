# Representations

## Owns

The explicit family of authored geometry models and the rules for converting between them with provenance.

## Does not own

A universal topology. Brush planes, manifold meshes, planar polygons, patches, and raw soup retain different invariants.

## First acceptance gate

Expose immutable representation views and checked conversion entry points without losing source IDs.
