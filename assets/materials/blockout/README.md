# Blockout material recipes

These twelve `.cymat` recipes pair the original procedural textures in
`assets/textures/blockout/` with the shared `tile_surface` shader. See that
texture directory's README for the catalog, generator, authorship, and license
information.

The paths are stable project resource identifiers. Existing maps may bind them
to any numeric material slot without depending on editor-only palette indices.
